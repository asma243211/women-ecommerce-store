function adminRender(){let ps=getProducts(),orders=JSON.parse(localStorage.getItem("lumera_orders")||"[]"),cart=getCart(),wish=getWish();document.getElementById("statProducts").textContent=ps.length;document.getElementById("statOrders").textContent=orders.length;document.getElementById("statWish").textContent=wish.length;document.getElementById("statCart").textContent=cart.reduce((a,x)=>a+x.qty,0);document.getElementById("adminProducts").innerHTML=ps.map(p=>`<div class="admin-row"><img src="${p.images[0]}"><div><strong>${p.name}</strong><small>${p.category} · ${money(p.price)} · Stock ${p.stock}</small></div><button onclick="editProduct(${p.id})">Edit</button><button onclick="deleteProduct(${p.id})">Delete</button></div>`).join("");document.getElementById("adminOrders").innerHTML=orders.length?orders.slice(0,8).map(o=>`<div class="order-row"><strong>#${o.id}</strong><span>${o.customer.name}</span><span>${money(o.total)}</span><em>${o.status}</em></div>`).join(""):"<p>No demo orders yet.</p>"}
const modal=document.getElementById("productModal"),form=document.getElementById("productForm");document.getElementById("addProductBtn").onclick=()=>{form.reset();form.id.value="";modal.classList.add("open")};document.getElementById("modalClose").onclick=()=>modal.classList.remove("open");
form.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(form)),ps=getProducts();if(d.id){let p=ps.find(x=>x.id==d.id);Object.assign(p,{name:d.name,category:d.category,price:+d.price,oldPrice:+d.oldPrice||0,stock:+d.stock,images:[d.image]})}else ps.push({id:Date.now(),name:d.name,category:d.category,price:+d.price,oldPrice:+d.oldPrice||0,rating:5,stock:+d.stock,badge:"New",popular:50,created:Date.now(),images:[d.image]});saveProducts(ps);modal.classList.remove("open");adminRender()};
function editProduct(id){let p=getProducts().find(x=>x.id===id);for(let k of ["id","name","category","price","oldPrice","stock"])form.elements[k].value=p[k]||"";form.elements.image.value=p.images[0];modal.classList.add("open")}
function deleteProduct(id){if(confirm("Delete this product?")){saveProducts(getProducts().filter(p=>p.id!==id));adminRender()}}
document.getElementById("resetDemo").onclick=()=>{if(confirm("Reset all products and remove demo orders?")){localStorage.removeItem(STORE_KEY);localStorage.removeItem("lumera_orders");adminRender()}};document.addEventListener("DOMContentLoaded",adminRender);